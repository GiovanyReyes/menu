/* global $ angular */
'use strict'

$(function() {
    // moment.js default language
    // moment.locale('en')

    angular.bootstrap(document, ['public'])
})
///tellign angular to attach angular to entire html